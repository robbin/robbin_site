<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, :list), url(:accounts, :index) %></li>
  <li> <%= link_to tag_icon(:list, :new), url(:accounts, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, :edit), url(:accounts, :edit, :id => @account.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :account, url(:accounts, :update, :id => @account.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'accounts/form', :locals => { :f => f } %>
    <% end %>
</div>
