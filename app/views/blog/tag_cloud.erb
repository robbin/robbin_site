<% @nav = 'tag' %>
<% @title = '所有Tag分类' %>
<% @description = '全站文章所有Tag分类的列表' %>

<div class="left">
  <div class="con">
  	<div class="tag_list">
  		<ul class="clearfix">
        <% Blog.cached_tag_cloud.each do |tag| %>
        <li>
          <%= link_to "<span>#{tag.name}</span>".html_safe, url(:blog, :tag, :name => tag.name), :class => 'tag', :rel => 'rel' %>
          X <span><%= tag.count %></span>
        </li>
        <% end %>
      </ul>
  	</div>
  </div>
</div><!-- left content -->

<div class="right">
  <%= partial 'blog/right' %>
</div><!-- right content -->
