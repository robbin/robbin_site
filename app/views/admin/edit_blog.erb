<div class="publish">
	<div class="con">
		<table class="content">
      <% form_for @blog, url(:admin, :blog, :id => @blog.id), :method => :put do |f| %>
      <%= partial 'admin/form', :object => f %>
      <% end %>
    </table>
  </div>
</div>